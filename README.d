    Replace the placeholders (client_id, redirect_uri, scope) with your actual OAuth details.
    Serve this HTML file via an HTTP server (e.g., you can use npx serve to serve the current directory).
    Navigate to http://localhost:3000 (or whatever port you set).